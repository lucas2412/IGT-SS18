pipeline {
  agent none
  stages {
    stage('Docker Build') {
      agent any
      steps {
        sh 'docker build -t server .'
      }
    }
	 stage('Docker Run') {
      agent any
      steps {
        sh 'docker run server -p 8080:8080'
      }
    }
  }
}